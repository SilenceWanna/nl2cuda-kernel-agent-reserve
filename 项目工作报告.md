# 项目工作报告：自然语言到 CUDA 前反向 Kernel 自动生成 Skill

报告日期：2026-08-20


## 一、项目背景与目标

### 1.1 问题背景

深度学习中大量算法最终都要落到 GPU 算子（CUDA Kernel）上执行。手写高性能 CUDA Kernel 门槛高、周期长，需要同时掌握算法本身、CUDA 编程模型、以及访存与并行的优化技巧；而 PyTorch 自带的 `torch.compile` 虽然能自动生成较优的融合算子，但在很多场景下并非最优，仍有可观的手写优化空间。与此同时，AI 编码 Agent（如 Claude Code、Codex、aider、gptme 等）的代码生成能力快速提升，具备了在人类方法论指导下自动完成"写代码—编译—验证—优化"闭环的潜力。

本项目要解决的核心问题是：**能否让用户只用一句自然语言描述算法，就由 AI Agent 自动产出一个正确、且性能超过 `torch.compile` 的 CUDA 前反向算子。**

### 1.2 项目目标

构建一套 **Agent Skill**（方法论文档 + 配套工具），驱动搭载它的 AI Agent 自动完成如下四步闭环：

1. **参考实现**：把自然语言算法描述翻译成一份可运行的 PyTorch 前向 + 反向参考实现，作为正确性金标准；
2. **Kernel 生成**：手写 CUDA 前向 Kernel 与反向 Kernel；
3. **优化迭代**：基于性能分析反馈，做 tiling、float4 向量化、warp 原语、算子融合、避免重算等优化；
4. **交付**：产出一个不依赖 PyTorch、可独立用 nvcc 编译的 `.cu` 文件。

**关键定位**：交付物是一套**宿主无关的 Skill**，而非从零训练或构建一个特定 Agent。方法论用自然语言写成、不绑定专有 API，工具是独立 Python 脚本，因此任何能读文档、执行 shell、访问 GPU 的 Agent 挂载后都能使用。这样做的价值在于——成果不依赖某一个 Agent 的实现，且能随底层大模型能力的进步而持续受益。

### 1.3 验收标准

以 PyTorch 参考实现为金标准，需同时满足三方面：

- **正确性**：不少于 5 组不同随机种子的输入，前向输出与反向梯度都满足 `allclose(atol=rtol=1e-2)`；
- **性能**：自研 CUDA Kernel 相对 `torch.compile`（默认 mode）基线，**前向与反向都要快至少 5%**（即 ≥1.05 倍）；
- **合规**：全程 fp32、不使用 fast-math、不降精度换速度；不落回 torch 高层算子；交付的 `.cu` 可独立编译。


## 二、系统设计

### 2.1 两层架构

为了让 Skill 做到"算法无关"、可无限扩展新算法，系统采用两层架构：

- **通用框架层（framework/，算法无关、对 Agent 只读）**：所有算法共享的评测底座，包括计时/容差协议、用例接口协议、正确性验证、性能计时、CUDA 编译加载、冒烟测试等模块。这一层对 Agent 只读，是防作弊设计的一环——Agent 不能通过修改评测规则来伪造达标。
- **算法用例层（cases/<算法名>/，每算法一份、Agent 在此写代码）**：每个算法一个独立目录，包含自然语言描述、PyTorch 参考实现、形状/参数配置、CUDA Kernel、autograd 封装、以及用例入口。

这套架构的核心收益是：**新增一个算法，只需新建一个用例目录，框架代码一行都不用改。** 这一点在项目早期就用一个与首个用例结构差异极大的新算法做了验证——框架零改动即支持，证明了架构的通用性不是设计愿景而是实测事实。

### 2.2 用例接口协议

每个算法用例通过一个统一的接口协议接入框架，必须提供固定的几项：算法名、自然语言描述、标量参数、需要求梯度的输入列表、数据类型、输入构造函数、以及 PyTorch 参考前向。这个协议是框架能对任意算法做统一验证和计时的基础；协议字段写错会直接导致验证崩溃，因此在方法论中被单列成一章重点强调（这一章正是在多宿主测试中，因某宿主接口写错而补强出来的）。

### 2.3 严格的计时协议（保证性能数字可信）

性能数字的可信度是本项目的生命线，计时协议做了严格设计：

- 使用 **CUDA events** 计时，而非 CPU 侧墙钟时间；
- 每次测量前后都做 `synchronize()`，确保测的是 GPU 上真实执行时间；
- **充分 warm-up**（预热若干次、不计入计时），把首次 JIT 编译、缓存冷启动、GPU 时钟升频等一次性开销挡在计时窗口之外；
- 正式测量多次、取**几何均值**；
- 重复多轮计算变异系数 CV，**CV 超过阈值自动作废重测**，降低共享 GPU 环境下的测量噪声；
- **前向与反向分别计时、各自达标**；反向计时时在计时区外重建计算图、只对 backward 计时，对 `torch.compile` 与自定义 autograd.Function 都公平。


## 三、执行过程与工作细节

项目历时约一个半月，按时间顺序推进。

### 3.1 调研与技术路线确定

项目从一开始就确定了做 Skill、而非训练模型的方向。在此前提下，为具体实现寻找可借鉴的范式，重点研读了字节跳动与清华团队开源的 **CUDA-Agent** 项目——业界首个用大规模强化学习训练、并在权威基准 KernelBench 上超过 `torch.compile` 以及 Opus、Gemini 等主流大模型的工作。它开源了训练数据集、专家撰写的 SKILL.md、以及一套"实现—编译—验证—分析—迭代"的标准化 Agent 工作区。这印证了本项目路线的两个关键判断：**约束 Agent 行为可以就用一份自然语言 SKILL.md**，**闭环工作范式是可迁移的骨架**。此外还系统阅读了该方向的一批代表性论文（多轮强化学习生成 Kernel、对比学习优化、推理图迁移优化经验、LLM 能否写高效 GPU Kernel 等），整理成一份中文综述作为知识沉淀。

### 3.2 搭建评测基础设施

先搭好算法无关的评测底座，它具双重身份：既是判定 Skill 是否达标的评测器，也是交给 Agent 的只读工具。这一步产出了正确性验证器、规范计时基准器、CUDA 即时编译加载器（支持多架构，如 T4 的 sm_75、A100 的 sm_80）、以及在无本地 GPU 时用于跑通全流程的 Colab 驱动 notebook。

一个关键的前置判断：在正式动手前，先在 GPU 上实测了 `torch.compile` 基线的耗时、并粗估手写 Kernel 的理论加速空间，确认所选用例上"快 5%"的目标可达，才进入下一阶段——避免在打不过的目标上空耗。

### 3.3 手写参考 Kernel 打通管线、沉淀第一版方法论

这一步是整个项目的地基。为写出可落地的第一版方法论，先手写了首个用例 RBF 核的前向和反向 Kernel：

- 朴素版正确性通过，但性能远不达标（前向约 0.32 倍、反向约 0.17 倍）——这符合预期，它的价值在于**验证了"编译→正确→计时→对比"整条评测管线可用**，并诊断出优化方向。

把手写过程中摸到的实操经验反向提炼，形成第一版 SKILL.md，主要包含：两层架构说明、用例接口协议、从自然语言到 Kernel 到自测的分步工作流、一个 **CUDA Kernel 优化技巧库**（涵盖反向梯度如何自主推导、数值稳定、规约模式、访存优化、occupancy 提升、避免重算与融合等，因为用户只给朴素描述、这些优化知识必须由 Skill 自身承载）、以及防作弊红线。随后用一个结构完全不同的新算法从零走通全流程，验证架构确实算法无关。

### 3.4 优化冲刺：首个用例达标

选定 RBF 高斯核矩阵（fp32，N=M=2048、D=64）为验收用例，在 Skill 指导下对前反向做了多轮针对性优化，过程本身也检验并丰富了技巧库：

- **前向**：从朴素的一线程一输出，先改为共享内存分块（tiling），实测几乎没提速——诊断发现瓶颈不是访存而是 **occupancy**（每 block 线程过多导致每 SM 只能驻留一个 block）；据此改为每 block 256 线程 + 每线程 2×2 输出微块（线程粗化）提升占用率，前向大幅提速；再叠加 float4 向量化。
- **反向**：决定性的一招是**把前向已算出的核矩阵缓存下来传给反向复用**，从而消除反向对距离和指数的重算、也消除了 block 内规约的同步开销。反向由此从最慢的短板变成最强的优势。

最终在 NVIDIA A100（sm_80）上达标：**前向 1.10 倍、反向 1.17 倍**，CV 均小于 1.5%，5 组随机种子正确性全部通过。随后产出了不依赖 PyTorch、可独立 nvcc 编译的交付版，`make test` 一键编译加 CPU 对拍通过、确认无 libtorch 依赖。**核心验收目标达成。**

### 3.5 降低使用门槛：内化方法论 + 自然语言确认闸门

早期使用仍要求用户给出前向数学公式加 shape，等于把数学建模的负担留给了用户。这一步做了两层改进：

- **内化方法论**：把测试中反复手输的要求（读方法论、自主推导反向、守红线、自测）内化进各 Agent 自动加载的约定配置文件，用户只需输入算法定义即可自动跑完全流程，不必每次手写一大段提示词。
- **自然语言确认闸门**：在流程最前面插入一道人类确认环节。用户用纯自然语言描述，Agent 先推导出结构化的数学规格（前向公式、输入输出 shape/dtype、对哪些输入求梯度），并在遇到多种合理解释时**显式列出所采取的解释和备选**（例如"归一化"是 LayerNorm 还是 L2、"注意力"要不要 causal mask），然后停下等用户确认，确认后才继续。这道闸门只设在数学层一次、不破坏后续自动化，既把数学建模责任从用户转移到了 Agent，又用一次确认兜住了自然语言的歧义，避免"数学理解错了却一路跑到 Kernel 才发现"。实测中，即便故意给出高度模糊的输入，Agent 也会主动暴露歧义点、把选择交还用户，而非擅自假设。

### 3.6 多维度打磨 Skill 通用能力（工作重心）

首个用例达标只证明功能可用，真正的工作量在于反复验证并提升 Skill 的通用性与可靠性。这一阶段从三个维度持续迭代。

**（1）多宿主兼容性验证。** 把同一套 Skill 分别接入 Codex、aider、gptme 等多个 Agent，用相同的自然语言输入让各宿主独立跑完整闭环，据暴露的问题反向补强方法论（前述"用例接口协议"章节即由此而来）。这一过程带来一个重要认识：**最终效果由三个相互独立的因素共同决定——Skill 方法论质量、底层大模型能力、宿主的自主性**，分析问题时必须分离归因，同一算法在最强宿主上通过、在较弱宿主上失败，往往是宿主能力差异而非方法论缺陷。实测各宿主能力呈明显梯度，其中 **Codex 最优**：以 conv1d 用例为例，三个宿主正确性都通过，但 Codex 一次跑通、无需迭代，反向加速比达 **5.42 倍**，明显高于 gptme 的 4.25 倍与 aider 的 1.98 倍，且主动补齐了规模配置与独立编译交付版；在归一化反向这类较难的用例上，Codex 能自主应用"多梯度融合进单 Kernel"做到 **1.48 倍达标**，而同一用例 gptme 仅 1.00 倍、未能突破。

**（2）以扩展算法形态驱动加速比提升。** 这一步的目的不是堆数量，而是用形态各异的算法不断给当前版本的 Skill 施压、暴露它在指导上的短板，再把新的解题思路沉淀回方法论，让它对后续算法越优化越好，形成"用例暴露短板、短板反哺方法论"的正向循环。几个典型的"诊断—突破"实践：

- **二维前缀和（积分图）**：初版按"一列一线程沿行串行累加"来写，只有 0.99 倍，一度以为是算法本征边界打不过。但性能分析发现瓶颈不在访存或算法，而在于这种写法并行度太低、大量计算单元闲置；改成指令级并行的流水写法（每线程同时处理多行、多个独立累加在流水线里并行推进、掩盖延迟）后，加速比一举翻到 **2.89 倍**。这个案例被抽象成方法论里一条重要准则：**"跑不赢"要先区分是本征边界还是优化不足，不要过早下结论。**
- **归一化反向的融合翻墙**：初版把多个梯度拆成多个 Kernel、每个各读一遍输入，只有 1.20 倍；重构成单个 Kernel 一次算完所有梯度、输入只读一遍后，访存减半、提到 **1.92 倍**。"多输出梯度融合进单 Kernel"由此沉淀为通用优化策略。
- **决策层减少盲试**：早期 Agent 面对归一化反向常盲目穷举多种写法都卡在打平线，后来把"先做性能分析、再按瓶颈类型对症选手段"写进方法论，Agent 不再乱试，而是诊断后直接命中有效优化。

累计以纯自然语言输入验证了 **27 个算法用例**，覆盖归约、矩阵乘、前缀扫描、卷积、位置编码、数据依赖写入、间接寻址、点积、分组归约、逐元素融合、空间采样、变长分段、线性求解、稠密分解、二维数据依赖 stencil、二维前缀和、warp 级稀疏聚合等维度。实测结果归纳为五个性能区间（详见第四节表格），并进一步沉淀出一套**能否超越 `torch.compile` 的元判据**：候选实现能赢，当且仅当满足三者之一——① 访存比基线更少（融合省去中间结果的反复读写）；② 用到了自动微分不知道的解析数学结构（如伴随系统、稳定递推）；③ 算术强度足够高，让计算而非访存成为瓶颈。三者都不占（如纯访存的前向、基线本身就是英伟达厂商库的情形），就诚实判定为本征边界、不再空转。一个有价值的规律是：**即便前向撞了带宽墙或厂商库墙，反向往往仍能大幅领先**，因为反向常含可利用的解析结构（如三对角求解反向可达近 10 倍）。这套判据让后续面对新算法时能快速预判性能上限、把精力投在真正有优化空间的地方。

**（3）保障评测可信度。** 加速比很容易被做假，重点防住两类会让数字虚高的陷阱：

- **短核虚高**：当基线耗时过小（小于 1 毫秒）时，Kernel 启动等固定开销占比过高会抬高加速比。应对手段是自适应把问题规模放大到计算主导区、并做规模敏感复测——原规模擦线通过、放大后掉破达标线的会被标记存疑。
- **弱基线失真**：如果 PyTorch 参考实现写得低效（用 Python 循环遍历张量维、伪向量化、按规模切换实现等），会让基线畸形慢、加速比虚高。应对手段是一个**静态预检脚本**，在跑 GPU 前就自动扫描参考实现里的这类危险写法并预警。

同时在工具层强制了不可违反的**防作弊红线**：待测路径禁止落回 torch 高层算子；官方库（cuBLAS/cuSOLVER 等）只能作辅助原语，禁止直接调用与目标算子等价的库成品（如实现 Cholesky 却直接调 cuSOLVER 分解函数——那样候选实现就等于基线，失去手写跑赢的意义）；评测脚本对 Agent 只读、进程隔离、不可篡改；不降精度换速度。这些红线也配了自动静态检查。

### 3.7 交付说明与独立编译交付版

编写了面向用户的完整使用说明书，覆盖从获取交付物到产出 `.cu` 的全流程，支持 Google Colab 和自备 GPU 两种路径、宿主无关。并把独立编译交付版的生成内化进了闭环——无论是达标的用例，还是被诚实认定为本征边界（带宽墙、厂商库墙）的用例，都会生成可独立编译的交付版，后者在说明中注明性能边界、不虚假宣称超过 `torch.compile`。


## 四、工作成果

### 4.1 27 个算法用例的实测结果

以纯自然语言输入验证了 27 个算法用例，加速比（相对 `torch.compile` 基线，前向 / 反向，取诚实基线下独立复验值）与所属性能区间如下：

| 算法用例 | 算法维度 | 前向 / 反向加速比 | 性能区间 |
|---|---|---|---|
| gated_ssm | 变系数递推 | 7.80 / 13.78 | 稳赢 |
| spmv | CSR 间接寻址 | 5.97 / 2.48 | 稳赢 |
| integral_image | 2D 前缀和 | 4.90 / 5.97 | 稳赢 |
| conv1d | 一维因果卷积 | 4.78 / 1.86 | 稳赢 |
| dynamic_grid_evolution | 2D 数据依赖 stencil | 4.63 / 5.26 | 稳赢 |
| warp_sparse_contrast | warp 级稀疏聚合 | 4.15 / 10.90 | 稳赢 |
| linear_ssm | 固定系数递推 | 3.90 / 4.60 | 稳赢 |
| scan | 前缀扫描 | 3.40 / 4.10 | 稳赢 |
| cosine_sim | 成对余弦 / 点积 | 2.12 / 1.42 | 稳赢 |
| gridsample | 数据依赖空间采样 | 2.11 / 2.26 | 稳赢 |
| segment_softmax | 变长分段 | 1.34 / 2.12 | 稳赢 |
| groupnorm | 分组归约 | 1.28 / 1.32 | 稳赢 |
| gemm_bias_gelu | 矩阵乘 + 融合 | 1.19 / 1.16 | 稳赢 |
| rbf | 距离 / 广播 | 1.10 / 1.17 | 稳赢 |
| tridiag | 串行依赖线性求解 | 1.27 / 9.80 | 稳赢（反向=解伴随系统） |
| softmax_ce | 归约 | 1.35 / 1.33 | 擦线 |
| temperature_softmax | 带温度 softmax | 1.11 / 1.70 | 擦线 |
| layernorm | 归约 | 1.09 / 1.11 | 擦线 |
| welford | 单遍统计 → 两遍归约 | 1.10 / 1.27 | 擦线 |
| rope | 位置编码 / 逐元素 | 1.02 / 2.59 | 擦线（前向带宽墙 / 反向真赢） |
| causal_attn | attention 融合链 | 1.26 / 1.30 | 宿主分层（仅最强宿主拿下） |
| scatter_add | 数据依赖写 + atomic | 2.19 / 1.20 | 宿主分层 |
| topk | 数据依赖控制流 | 1.35 / 1.33 | 宿主分层 |
| maxpool | 2D 空间 + argmax | 1.02 / 4.58 | 带宽墙（前向）/ 反向真赢 |
| geglu | 融合逐元素算子链 | 1.02 / 2.36 | 带宽墙（前向）/ 反向真赢 |
| l2norm_scale | L2 归一化 + 缩放 | 1.01 / 1.48 | 带宽墙（前向）/ 反向真赢 |
| cholesky | 稠密分解 | 0.31 / 1.25 | 厂商库墙（前向）/ 反向真赢 |

### 4.2 五类性能区间与核心方法论结论

上述实测被系统归纳为五个性能区间，这是本项目最有价值的经验产出：

- **稳赢区**：距离、扫描、卷积、状态空间模型、间接寻址、点积、大规模归约、数据依赖采样、变长分段等。手写融合空间大、或用到了解析结构，能稳定数倍领先。
- **擦线区**：小规模归约类的归一化前向（LayerNorm、welford、temperature_softmax 等）。`torch.compile` 已接近最优，加速比在 1.05~1.10 之间抖动、放大规模后容易掉，必须在计算主导区多次复验。
- **宿主分层区**：融合密集的算法（attention、GEMM 融合、scatter、topk）。成败取决于 Kernel 实现力和基线诚实度，往往只有能力最强的宿主能拿下。
- **带宽墙区**：纯访存、低算术强度的前向（maxpool、geglu、l2norm 前向）。候选实现访存量等于基线、都已达峰值带宽，多宿主一致无法跑赢，属算法本征限制、前向无优化空间——这是被诚实识别出的边界，不是失败。
- **厂商库墙区**：前向基线本身就是英伟达厂商成品（Cholesky 对应 cuSOLVER）。手写极难超越，正确做法是承认边界、手写尽力、诚实报告。

由此提炼出的两条核心结论：一是前述**能否跑赢的元判据**（访存更少 / 有解析结构 / 算术强度够高，三者居其一才能赢）；二是**反向通常比前向更容易跑赢**——前向多是数据搬运、受带宽限制，反向常含 Jacobian、散回、伴随系统等可融合利用的复杂计算。

### 4.3 交付物清单

- **通用框架**：算法无关的评测底座（计时/容差协议、用例协议、正确性验证、性能计时、编译加载、冒烟测试）。
- **算法用例**：27 个已验证的算法形态，每个含自然语言描述、PyTorch 参考、CUDA 前反向 Kernel、autograd 封装、以及可独立编译的 `.cu` 交付版。
- **Skill 主体**：SKILL.md 方法论（含优化技巧库与瓶颈诊断决策表）、迭代优化循环说明、配套 CLI 工具（正确性验证、性能计时、性能分析、参考实现静态预检）、跨宿主测试矩阵（累计 45 轮记录）、逐用例实测例证库。
- **使用文档**：从获取交付物到产出 `.cu` 的完整说明书，支持 Colab 与自备 GPU 两种路径、宿主无关。
- **参考文献**：10 篇公开论文及一份中文综述。
- **工程质量保障**：短核假象自适应放大复测、弱基线静态预检、防作弊红线自动检查、擦线用例连续 3 次复验的稳定性判据、远程 GPU 自测驱动脚本的健壮性处理等。
- **版本管理**：累计约 191 次提交，从 2026-07-06 持续演进至今。

### 4.4 成果总结

- **核心验收目标达成**：在选定用例 RBF 上前反向均超过 `torch.compile` 5% 的硬指标，正确性全部通过，并产出可独立编译的纯 CUDA 交付版。
- **通用性充分验证**：27 个不同形态的算法用例、多个宿主、累计 45 轮跨宿主测试，证明 Skill 是可迁移的方法论、而非针对单一算法的技巧。
- **边界被诚实厘清**：五个性能区间明确划分了哪些算法能赢、哪些是本征边界，并给出能否跑赢的元判据，使 Skill 面对打不过的情况会诚实报告边界、而非空转或造假。
- **交付物完整、开箱可用**：方法论、工具、每个用例的独立编译交付版、使用文档齐全，宿主无关。


## 五、总结与展望

本项目完整走通了"自然语言 → PyTorch 参考 → CUDA 前反向 Kernel → 优化 → 独立交付 `.cu`"的自动化闭环，并通过 27 个算法用例、多宿主、45 轮测试建立起一套可复用、可迁移、且对自身边界诚实的方法论。

从工程价值看，最有意义的沉淀不只是"跑赢了多少算子"，而是**一套判断何时能赢、如何去赢、何时该诚实认输的决策体系**，以及一整套保证性能数字真实可信的评测防护。

当前方案也有明确的局限：Skill 的效果高度依赖人工沉淀的专家经验，不同 Agent 宿主的输出质量差异明显。后续可进一步丰富算子优化范式、增强 Agent 自主的性能诊断与问题定位能力，降低对人工经验的依赖，让 Skill 在更弱的宿主上也能稳定发挥。
