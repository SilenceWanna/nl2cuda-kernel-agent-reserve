# 成对余弦相似度矩阵（cosine_sim）

计算两批向量之间的成对余弦相似度矩阵。

## 前向

输入 A[N,D]、B[M,D]（fp32）。先各自 L2 单位化，再两两点积：

    S[i,j] = (A[i] · B[j]) / (|A[i]| · |B[j]|)

输出 S[N,M]（fp32），每个元素 ∈ [-1, 1]。

## 输入

- A: [N, D]，fp32。
- B: [M, D]，fp32。
- eps: 防除零，1e-8。

## 梯度

对 A 和 B 求梯度。
